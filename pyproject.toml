[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mistmind"
version = "0.1.0"
authors = [{ name = "Cheenu", email = "cheenu1092@gmail.com" }]
description = "Intelligent MCP Server for Juniper Mist API with semantic tool design"
keywords = ["Mist", "Juniper", "MCP", "LLM", "AI"]
license = { text = "MIT License" }
readme = "README.md"
requires-python = ">=3.12"
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
  "Intended Audience :: System Administrators",
  "Topic :: System :: Networking",
  "Development Status :: 3 - Alpha",
]
dependencies = [
  "mcp[cli]>=1.9",
  "pydantic>=2.0",
  "pydantic-settings>=2.0",
  "python-dotenv>=1.0.0",
]

[project.urls]
"Source" = "https://github.com/cheenu1092-oss/mistmind"

[project.scripts]
mistmind = "mistmind.__main__:main_sync"

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0",
  "pytest-asyncio>=0.23.0",
  "pytest-cov>=4.1.0",
  "ruff>=0.1.0",
]

[tool.hatchling.build.targets.wheel]
packages = ["src/mistmind"]

[tool.hatchling.build.targets.sdist]
include = ["/src", "/tests", "/README.md", "/pyproject.toml"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
asyncio_mode = "auto"
addopts = [
  "-v",
  "--tb=short",
  "--cov=src/mistmind",
  "--cov-report=html",
  "--cov-report=term-missing",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.coverage.run]
source = ["src/mistmind"]
omit = ["*/tests/*", "*/__pycache__/*"]
